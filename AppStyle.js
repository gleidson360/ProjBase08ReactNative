import { StyleSheet } from "react-native";

export default StyleSheet.create({
  tela: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },

  texto: {
    color: "#fff",
    fontSize: 20,
    fontWeight: "bold",
  },

  camera: {
    height: "100%",
    width: "100%",
  },
});
